<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="2dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <include layout="@layout/view_item_grid_basic"/>
    <TextView
        android:textSize="12dp"
        android:textColor="@android:color/black"
        android:gravity="center"
        android:id="@+id/commentCount"
        android:background="@drawable/thumbnail_mark_comment"
        android:paddingBottom="6dp"
        android:visibility="gone"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_alignLeft="@+id/photoView"
        android:layout_alignBottom="@+id/photoView"/>
    <ImageView
        android:id="@+id/commentMedia"
        android:visibility="gone"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/thumbnail_mark_wave"
        android:layout_alignRight="@+id/photoView"
        android:layout_alignBottom="@+id/photoView"/>
</RelativeLayout>
